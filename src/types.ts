export interface PathInfo {
  emoji: string;
  title: string;
  desc: string;
  tag: string;
  tagText: string;
  cause: string;
  benefit: string;
}

export interface CourseDetail {
  name: string;
  full: string;
  origin?: string;
  category?: string;
  focus: string;
  why: string;
}
